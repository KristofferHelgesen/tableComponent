import React from "react";
import Icon from "@mdi/react";
import { mdiChevronDown, mdiChevronUp } from "@mdi/js";

export const arrowDown = <Icon size={1} path={mdiChevronDown} />;

export const arrowUp = <Icon size={1} path={mdiChevronUp} />;
